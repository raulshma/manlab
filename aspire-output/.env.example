# This file is a template for the Compose bundle produced by:
#   aspire publish -o aspire-output
#
# Copy to .env and fill in values:
#   aspire-output/.env.example -> aspire-output/.env

# Postgres password for the 'postgres' service (required)
PGPASSWORD=

# Container images for the services.
# If you're using images published by GitHub Actions (GHCR), set these to the pushed tags.
# Example:
#   SERVER_IMAGE=ghcr.io/<owner>/manlab-server:sha-<shortsha>
#   WEB_IMAGE=ghcr.io/<owner>/manlab-web:sha-<shortsha>
SERVER_IMAGE=
WEB_IMAGE=

# Public port metadata for the server (used by generated endpoint metadata)
# If unsure, leave as 8081.
SERVER_PORT=8081
